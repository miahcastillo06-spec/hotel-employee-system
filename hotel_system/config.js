// ==========================================================
// Point this at the Employee System's API endpoint.
// If both systems run on the same computer via XAMPP/localhost,
// this is usually enough. If Employee System is on a separate
// physical PC (PC2), replace "localhost" with PC2's IP address,
// e.g. "http://192.168.1.20/employee_system/api_get_employees.php"
// ==========================================================
const EMPLOYEE_API_URL = "http://localhost/hotel-employee-system/employee_system/get_employees.php";
